# TODO FOR BACKEND

1. Пробежаться по всем сервисам, перепроверить саму логику, привести все к одному стандарту
2. gayteway-service:
   1. Ввести стандарт для отправки ответов. Добавить логические структуры, создать SWAGGER для удобства работы с API сервера
   2. Рефактор всего кода, убрать затычки
3. report-service:
   1. Придумать хорошее и логичное логгирование
   2. Рефактор всего кода, убрать затычки
4. report-worker:
   1. Придумать хорошее и логичное логгирование
   2. Рефактор всего кода, убрать затычки
5. auth-service:
   1. Придумать хорошее и логичное логгирование
   2. Рефактор всего кода, убрать затычки
6. MINIO:
   1. Решить вопрос с удалением отчетов (поставить лимит и создать воркер на это)
7. Буду дополнять


# TODO FOR FRONTEND

1. Пробежаться по всем файдам, перепроверить саму логику, привести все к одному стандарту
2. Починить цвета в:
   * Выбор формата (серая кнопка)
   * Изменить цвет для кнопки "выйти"
3. Убрать кнопку "предпросмотр" + отредактировать 6 пункт (сделать покрасивше комбо скачать + лимит строк)
4. Кэшированные запросы:
   1. Починить проблему с подсосом комментариев для кэшированных запросов: понять проблем в беке или на фронте и решить
   2. Починить удаление отчетов на сайте, ибо на сервере удаляются, но на фронте нет
   3. Добавить статусы для отчетов. Они должны проверяться до тех пор, пока идет состояние RUNNING
      1. Статусы должно перепроверяться каждые 2-5 секунд (обсудить)
   4. Добавить модальное для кэшированных запросов, где будут отображаться следующие пункты:
      1. Имя + комментарий + время создания отчета
      2. Формат отчета
      3. Статус отчета
      4. Кнопки: восстановить отчет (для возвращения на фронте параметров создания отчета) и скачать отчет (если статус == COMPLETED)
      5. Для админов добавить sql панель где хранится изначальный sql
   5. Обработка ошибок:
      1. Свериться с беком и стандатизировать все ошибки. Некоторые новые ответы не обрабатываются на фронте и выдаются сырые результаты (так не должно)
   6. Создание графичиеских отчетов:
      1. Решить вопрос нового алгоритма создания визуальных отчетов. Старый не работает (отчеты больше сразу не возвращаются)
   7. Будет что-то еще...
